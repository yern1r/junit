public class TreeSet {
    //first element is - head
    //then relatively to this head element
    //we will define the next will go the right side or left
    //if next element is greater than first element
    // then it goes right(down-right) side
    //if next(next) element is less than next element
    //then it goes to left(down-left) side
    //as result we will have picture of tree

    //in the adding of element we will add to the latest element
    // and the path we will count by steps which is also height
    // add/remove/contains - O(log(N))
    // O(1) < O(log(N)) < O(N)
}
